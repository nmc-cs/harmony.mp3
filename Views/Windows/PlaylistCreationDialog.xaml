<Window x:Class="Harmony.Views.Windows.PlaylistCreationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create New Playlist" Height="170" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E1E">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Enter playlist name:" Foreground="White" FontSize="14" Margin="0,0,0,10"/>

        <TextBox Grid.Row="1" x:Name="PlaylistNameTextBox" Margin="0,0,0,20" 
                 Padding="5" Background="#2E2E2E" Foreground="White" 
                 BorderBrush="#444444" FontSize="14"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="Cancel" Width="80" Height="30" 
                    Margin="0,0,10,0" Background="#2E2E2E" Foreground="White" 
                    BorderBrush="#444444" Click="CancelButton_Click"/>

            <Button x:Name="CreateButton" Content="Create" Width="80" Height="30" 
                    Background="#6200EE" Foreground="White" BorderBrush="#6200EE"
                    Click="CreateButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
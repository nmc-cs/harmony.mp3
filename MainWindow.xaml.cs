using System.Windows;

namespace Harmony
{
    public partial class MainWindow : Window
    {
        public MainWindow()
        {
            InitializeComponent();

            // We'll implement the ViewModel binding later
            // after we get the basic UI working
        }
    }
}